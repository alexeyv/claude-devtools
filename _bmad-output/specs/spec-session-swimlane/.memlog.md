---
topic: Session swimlane: wall-clock view of parent, subagents, and HITL
updated: 2026-08-16T02:16
---

- (direction) Source: in-chat statement of intent for a session swimlane on the existing session view. Not a restyle; turn microscope stays.
- (decision) Slug session-swimlane. Create spec at _bmad-output/specs/spec-session-swimlane/. Rich input: extract directly, no elicitation.
- (note) Why: DevTools is strong inside a turn and weak at interplay. Skill runs (especially BMAD) are orchestrations: parent parks, children run in parallel, HITL is a short gap not the wall.
- (capability) CAP-1 Toggle: user switches a loaded session between the existing turn list and a wall-clock swimlane from session-view chrome. Per-tab, off by default, no Settings.
- (capability) CAP-2 Parent lane: intervals the parent was producing (assistant/tool work) vs silent, on one wall clock from earliest to latest timestamp in parent + resolved children.
- (capability) CAP-3 Child lanes: one lane per spawned Agent/Task child, bar from first to last timestamp in that child jsonl. Parallel children overlap. Nested children are extra rows under their parent, not a second page.
- (capability) CAP-4 HITL marks: ask and answer as marks; gap between them is a wait, not parent work. Inferred from AskUserQuestion, a real user message after parent quiet, or a headless claude -p checkpoint/resume.
- (capability) CAP-5 Profile numbers: each bar shows wall duration plus uncached input, cache read, cache write, output, without opening JSONL. Child bars use that child's own usage, never parent mainSessionImpact.
- (capability) CAP-6 Click-through: parent interval opens that turn in the existing chat view; child bar opens existing subagent detail (expandSubagentTrace). Do not duplicate the microscope.
- (capability) CAP-7 Agent-as-spawn: treat Claude Code Agent tool calls as child spawns, not only Task. Prefer parentTaskId / spawn tool id join; timing fallback (child startTime inside parent chunk) is last resort.
- (constraint) One Claude data root, one session tree. No cross-home stitching (orchestrator in ~/.claude launching claude -p into /tmp/.../claude-home is two roots, out of scope).
- (constraint) Add a projection buildSwimlane(chunks, processes, messages), not a second session model. Use SessionParser, SubagentLocator/SubagentResolver, ProcessLinker, and Process (startTime, endTime, parentTaskId, metrics).
- (constraint) Do not edit ChunkBuilder except as required to classify Agent as a spawn. Do not restyle ChatHistory. Do not add a new parser or a third interpretation of unofficial jsonl; inherit existing drift handling.
- (constraint) Child file paths go through the existing locator. Do not hardcode projects/<id>/subagents/ vs projects/<id>/<sessionId>/subagents/.
- (constraint) A silent stretch must never look like work. Wrong wait-class is acceptable if the gap is still drawn.
- (constraint) Isolate: (1) pure lane-model function testable without Electron; (2) small view reusing existing surface colors and type, no new design system; (3) one button, tab-local toggle like Context panel, off by default; (4) click-through calls existing navigation.
- (constraint) HITL is inferred, not a log type. Label the mark; do not pretend certainty. Headless checkpoint = last assistant in a segment, then a later user string that is not a tool_result and not isMeta.
- (decision) Default remains the turn list. Swimlane is opt-in, per tab, no settings, no theme work.
- (decision) Companions: interval-classes.md (parent interval classes + HITL inference) and brownfield.md (existing types, locator, join, allowed edits, token source, navigation).
- (assumption) Uncached input maps to existing inputTokens; cache write maps to cacheCreationTokens / cache_creation_input_tokens; cache read and output map to cacheReadTokens and outputTokens.
- (assumption) Child bar tokens may be taken from Process.metrics when that object is the sum of assistant usage in that child jsonl.
- (assumption) Tab-local visibility follows the existing showContextPanel pattern in tabUISlice, not Settings.
- (question) Exact chrome slot: SearchBar strip vs sticky Context button row? Input said same strip as Context / search, but those are two places in the current UI.
- (question) Does the swimlane replace the turn list while on, sit above it, or overlay? Click-through to the existing chat view must still work.
- (question) Is Agent-as-spawn limited to isTask (toolExtraction + SubagentResolver + ProcessLinker), or must renderer Task-name checks (displayItemBuilder, ConversationGroupBuilder) also treat Agent as a spawn?
- (question) How is nesting detected for extra rows? Process has parentTaskId (spawn tool id), not a parent-process id.
- (note) Demo shape named by input: a session that spawned parallel Agents and had a checkpoint/resume (bmad-code-review run 1).
- (note) Non-goals from input: ChatHistory/token/context/subagent-modal redesign; new parser; cross-home stitch; skill-run comparer / run-vs-run / SKILL.md cost; live tail; live steering; Cogpit; custom themes; any improvement to existing turn UI.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1..CAP-7 each have intent+success; intents are WHAT; constraints rule things out; non-goals explicit; success signal is a demonstrable session; IDs unique; prose lean. Open questions left as gaps rather than invented answers.
- (event) Self-validate pass 2 (preservation): PASS. Load-bearing claims landed in SPEC.md, interval-classes.md, or brownfield.md. Wrapper-only dropped: repo/fork URL, rhetorical framing around the pain, isolation-list ceremony (content kept).
- (event) Spec derived. Folder _bmad-output/specs/spec-session-swimlane/. 7 capabilities, 2 companions (interval-classes.md, brownfield.md).
- (direction) User: no view until they see a solution; decide the four open questions.
- (decision) Chrome slot: sticky top-right session-view control row in ChatHistory, same button language as Context. Always render that row so the swimlane toggle is visible even when Context is hidden. Not SearchBar: SearchBar is Cmd+F only and would hide the button.
- (decision) Swimlane replaces the turn list while on. Click-through turns the swimlane off and calls existing navigation (scroll to turn / expandSubagentTrace). No overlay, no stacked pane — both would hide the microscope or redesign the session layout.
- (decision) Agent-as-spawn includes every check that decides a linked Process is a subagent: isTask in toolExtraction, SubagentResolver, ProcessLinker, and name===Task gates in displayItemBuilder and ConversationGroupBuilder. Not a ChatHistory restyle; SubagentItem appearance unchanged. contextTracker label follows isTask.
- (decision) Nesting: process B is a child-of-child when B.parentTaskId matches a spawn tool_use id in process A messages. Render B as an extra row under A. parentUuid continuation-chain files are the same child: one row, bar from earliest start to latest end, not extra rows.
- (constraint) CAP-1 placement: ChatHistory sticky top-right row, always visible, Context-button language.
- (constraint) CAP-6: replacing the list while on means click-through must dismiss the swimlane so the existing microscope is visible.
- (event) Self-validate pass 1 (coherence) after resolving open questions: PASS. Open Questions section removed. CAP-1/3/6/7 success criteria updated to carry the four decisions. Assumptions unchanged.
- (event) Self-validate pass 2 (preservation) after resolving open questions: PASS. Four decisions landed in SPEC.md plus interval-classes.md (nesting/continuations) and brownfield.md (chrome slot, replace-then-navigate, Agent-as-spawn file list).
- (direction) User: keep the four open-question resolutions; decide remaining breakdown judgment; break into stories.
- (decision) Story 1 Agent-as-spawn join (CAP-7). Prerequisite so the lane is not a lie. Independently reviewable in the existing microscope. spec_checkpoint false, done_checkpoint false.
- (decision) Story 2 buildSwimlane projection (CAP-2, CAP-3, CAP-4, CAP-5 data). Pure function + tests, no Electron. spec_checkpoint false, done_checkpoint false.
- (decision) Story 3 Toggle chrome (CAP-1). Sticky top-right button, per-tab, replaces turn list with the swimlane surface. spec_checkpoint false, done_checkpoint false.
- (decision) Story 4 Draw the clock (CAP-2..CAP-5 visual). Axis, rows, bars, HITL marks, token numbers, distinct wait classes. spec_checkpoint false, done_checkpoint false.
- (decision) Story 5 Click-through (CAP-6). Dismiss swimlane then existing navigation. spec_checkpoint false, done_checkpoint true so the human sees the end-to-end result before anything further.
- (decision) Checkpoints: no per-story spec pause (user wants the end result). done_checkpoint only on story 5.
- (event) Story Breakdown validity: PASS. Five stories, ids 1-5 quoted and prefix-free, required fields present, no status. stories.yaml written beside SPEC.md, not listed in companions.
- (direction) User asked to add a thin UX companion. Not a full bmad-ux DESIGN/EXPERIENCE pass.
- (decision) Companion swimlane-visual.md: map interval classes, marks, button, metrics density, and empty lane onto existing CSS variables. No new tokens, no new design system.
- (decision) Button label is Swimlane. Same --context-btn-* active/idle language as Context.
- (decision) Work = solid --color-surface-raised. Child-wait = hatch/stripe on --color-border-emphasis, not a new hue. HITL-wait = --warning-* (not --interruption-*, which is abort-red). Idle = empty track, --color-border-subtle only.
- (decision) HITL marks are ticks plus a short inferred label (ask / resume / checkpoint), --warning-text. They are not bars.
- (decision) Each bar carries one compact metrics line: formatDuration plus four formatTokensCompact figures (uncached, cache read, cache write, output). If the bar is too short, the line sits to the right of the bar. Hover may repeat; it does not hide the four numbers.
- (decision) No zoom or pan. Overflow is horizontal scroll inside the swimlane surface. Same content column as ChatHistory. Child row labels reuse Process description/subagentType and existing team/type color helpers when present.
- (decision) No-children: one parent row, axis, HITL marks if any. No empty-state illustration.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1/4/5 cite swimlane-visual.md. No new CSS variables. Open questions still empty.
- (event) Self-validate pass 2 (preservation): PASS. Visual decisions landed in swimlane-visual.md; SPEC companions list now interval-classes.md, brownfield.md, swimlane-visual.md. stories.yaml descriptions still match; not rewritten (update does not rewrite stories).
- (direction) User: go on — apply the ranked spec fixes from the Codex review triage (interval partition, HITL pairing + wait precedence, nesting join + continuation segments, main-process projection, error-nav click-through). Leave 7 as an implementation note; 9 as a brownfield warning.
- (decision) Parent work is a partitioned interval, not a timestamp and not gap-fill-to-next. Group parent assistants by owning AI chunk; split a chunk's assistant run wherever a HITL ask is open; each remaining run is work [first assistant, last assistant]. Tokens on that interval are those assistants' usage only.
- (decision) HITL pairing: AskUserQuestion ask = tool_use, answer = matching tool_result/toolUseResult for that tool_use id — not the next real user message. Resume/checkpoint end at isParsedRealUserMessage or a later non-meta non-tool_result user string. Headless checkpoint is inferred from parent messages only; no process-end field on buildSwimlane.
- (decision) Wait-class precedence: while an ask is open, HITL-wait wins. Child-wait is only parent-silent + child timestamps in the gap + no open HITL. Else idle. No fourth combined class. Wrong wait-class is acceptable only for leftover gaps with no ask pair and no child timestamps. Required fixtures: child-only wait, explicit HITL, idle, overlapping HITL+children (must be HITL-wait).
- (decision) Nesting is computed by buildSwimlane from process messages, not assumed from resolver parentTaskId. Prefer spawn tool_result agentId === child.id; then parentTaskId matching a spawn id in some process's messages. Nested spawn wins over a root positional parentTaskId. Acceptance fixture: at least two nesting levels.
- (decision) Continuations (parentUuid chain): one row, rowId = primary process id (spawn-matched / earliest with parentTaskId). One bar per activation Process (that JSONL's startTime–endTime). Gaps between activations are empty, not child work. Metrics sum each segment's Process.metrics with no double-count. Click opens the clicked segment's process id.
- (decision) buildSwimlane lives in main analysis (src/main/services/analysis/), runs on chunks + processes-with-messages + parent messages before the IPC strip, and serializes a SwimlaneModel onto SessionDetail. Do not implement it as a renderer util against stripped messages. Integration criterion: renderer receives all intervals, rows, nest edges, HITL marks, and token lines without reading session.messages or Process.messages.
- (decision) CAP-6 click-through: dismiss swimlane, then the existing error-nav sequence — parent: ensureGroupVisible + scroll to that turn; child: expand owning AI group, expand the outer SubagentItem, expandSubagentTrace, ensureGroupVisible, scroll. Do not call expandSubagentTrace alone. Lane model carries turn/group targets. Cover collapsed and virtualized destinations.
- (constraint) A silent stretch must never look like work. Wrong wait-class is acceptable only when the gap has no open HITL pair and no child timestamps in range.
- (constraint) Do not gate the Swimlane toggle or child lanes on hasSubagents / initial session.hasSubagents (NEW layout only). listSubagentFiles / resolved Process[] is authoritative and covers old and new layouts.
- (note) Finding 7 (SemanticStep/LinkedToolItem lack isTask) is an implementation note, not a spec change: CAP-7 and story 1 already require displayItemBuilder, ConversationGroupBuilder, and contextTracker to follow isTask. A shared spawn-name predicate is enough.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1..7 still have intent+success; IDs unchanged; wait-class constraint now falsifiable; success signal includes HITL-wins-on-overlap and expanded child click-through.
- (event) Self-validate pass 2 (preservation): PASS. Partition, HITL pairing, precedence, nest join, continuation segments, main-process SwimlaneModel, error-nav click-through, and hasSubagents warning landed in SPEC.md / interval-classes.md / brownfield.md / swimlane-visual.md.
- (note) stories.yaml stale vs this update. Story 2 still says nesting is parentTaskId-only and does not name partition/HITL pairing/continuation segments/main placement. Story 5 still says expandSubagentTrace / expandSubagentTraceForTab alone. Update does not rewrite stories.yaml — offer Story Breakdown.
- (direction) User: yes — re-run Story Breakdown so stories match the updated contract. Keep the five-story slice and existing checkpoint judgments.
- (decision) Story 1 Agent-as-spawn (CAP-7) unchanged. spec_checkpoint false, done_checkpoint false. Carried forward.
- (decision) Story 2 Project the lane model (CAP-2, CAP-3, CAP-4, CAP-5 data): buildSwimlane in main analysis emits partitioned work, nest edges, continuation segments, paired HITL marks, token numbers as SwimlaneModel on SessionDetail. Fixtures and pairing in interval-classes.md. spec_checkpoint false, done_checkpoint false.
- (decision) Story 3 Toggle chrome (CAP-1) unchanged. spec_checkpoint false, done_checkpoint false. Carried forward.
- (decision) Story 4 Draw the clock (CAP-2..CAP-5 visual) unchanged as a slice; dispatch note now includes continuation gaps empty and HITL-wins-on-overlap. spec_checkpoint false, done_checkpoint false.
- (decision) Story 5 Click-through (CAP-6): dismiss then error-nav sequence (expand group, outer card, trace, ensure visible, scroll). Not expandSubagentTrace alone. spec_checkpoint false, done_checkpoint true.
- (event) Story Breakdown validity: PASS. Five stories, ids 1-5 quoted and prefix-free, required fields present, no status. Checkpoints carried forward (done_checkpoint only on 5). stories.yaml re-derived; 2 and 5 now match the contract, 4 dispatch note covers continuation gaps and HITL-wins-on-overlap.
- (direction) User: stop overengineering the spec into an implementation. Codex habit. Stop before making choices a coding agent or story-planning one should make.
- (decision) Work is parent producing. Waiting on a spawned child is silence (then classified), even when the next parent assistant is still in the same AI chunk. Do not gap-fill to the next timestamp. Do not specify the split algorithm.
- (decision) Parent usage inherits the existing requestId / calculateMetrics dedup. Do not invent a second token sum.
- (decision) Click-through only for bars that already have a microscope target (parent work → that turn; child with an existing card → that card). Silent parent intervals are not clickable. Nested rows without a card stay visible. Do not invent a second microscope or prescribe the nav function sequence.
- (decision) AskUserQuestion is a paired ask/answer (tool_use to matching tool_result). A later real user message after silence is resume. Collapse checkpoint into resume — messages cannot distinguish them. Do not require a process-end field or a new discriminator.
- (decision) Nest children under the process that spawned them, using the same spawn-join rules already used for root children (including team members), applied to each process's messages. Do not trust resolver parentTaskId. Do not write the matching algorithm.
- (decision) Strip partition steps, nest-match steps, rowId fallbacks, error-nav function lists, metrics-collision rules, and answer-label treatments from the contract. Those are implementation or story-planning.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1..7 still have intent+success; work-vs-silence and click-target rules are WHAT; function sequences and match algorithms removed.
- (event) Self-validate pass 2 (preservation): PASS. Demo-case child-wait, HITL pairing, main-process projection, continuation segments, nest-under-spawner, existing-microscope-only click, and consume-existing-dedup all remain. Checkpoint class dropped (collapsed to resume). Algorithms left for implementer.
- (note) stories.yaml not rewritten. Story 2/5 descriptions still name the same slices; their invoke_dev_with still lists partition/error-nav HOW that the spec just dropped. Offer to slim those dispatch notes.
